[project]
name = "footage-studio"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "jinja2",
    "python-multipart",
    "ffmpeg-python",
    "static-ffmpeg",
    "opencv-python-headless",
    "wakepy",
    "scipy>=1.17.1",
    "stitching",
    "matplotlib>=3.10.8",
]

[project.scripts]
footage-studio = "footage_studio.main:main"
export-extrinsics = "footage_studio.stitching.export_extrinsics:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "httpx",
    "ruff",
    "mypy",
]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
